# v1 legacy artifacts

These files belong to the original submission preserved at git tag
`v1-original-submission`. They are kept only so reviewers can inspect the
pre-remediation search and bootstrap outputs without checking out that tag.

They are **not** authoritative for the remediated model.

Authoritative remediation artifacts live in `artifacts/current/`.
